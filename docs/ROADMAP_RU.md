# ROADMAP (RU)

Актуально на: 2026-02-04 20:00 UTC

Формат: Now / Next / Later. Каждый пункт должен иметь критерий готовности (DoD) и ссылку на PR/issue, если есть.

## Now
- Стабилизировать структуру репозитория (убрать дубли, единый вход docs/).  
  DoD: нет дублирующих пакетов/индексов; handbook лежит в `docs/handbook/`; CI docs policy соответствует реальной структуре.

## Next
- Команда `/categories`: синхронизация каталога из Яндекс.Диска в SQLite.  
  DoD: `/categories` показывает список (<=50) и обновляет `catalog_items`; скачивание через `/download` работает по элементам из этого каталога.
- Локальный режим end-to-end (`STORAGE_MODE=local`): `/seed` создаёт demo файл на диске и его можно скачать через `/download`.  
  DoD: сценарий из README выполняется без ручных правок файлов.
- Минимальные e2e smoke-tests в CI (контейнеры поднимаются, `/health` отвечает).  
  DoD: workflow или отдельный job, который гоняет docker compose + curl.

## Later
- Пагинация/поиск по каталогу (если каталог большой).  
  DoD: пользователь может получить следующий “экран” без таймаута/лимитов Telegram.
- Роли/права (admin/moderator) через таблицы roles/user_roles.  
  DoD: команды, требующие прав, не доступны обычным пользователям; права выдаются админом.
- Инциденты/наблюдаемость: алерты по метрикам, минимальный дашборд.  
  DoD: известные SLO и алертинг на downtime/ошибки отправки.

## История изменений
| Дата/время (UTC) | Автор | Тип | Кратко | Commit/PR |
|---|---|---|---|---|
| 2026-02-04 20:00 UTC | ChatGPT | doc | Заполнен roadmap по фактическому состоянию репозитория | |
